import 'package:coffee_badger/app.dart';
import 'package:flutter_test/flutter_test.dart';

// TODO: add ratio page UI elements validation.
// TODO: 1. Make sure UI elemts shown/hided when user interacts.
// TODO: 2. Make sure the UI elements show correct data.

void main() {
  // testWidgets('Ratio page', (WidgetTester tester) async {
  //   await tester.pumpWidget(App());

  //   expect(find.text('Ratio calculator'), findsOneWidget);
  // });
}
